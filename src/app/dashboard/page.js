import { Header } from "../components/header";
import { Form } from "./Form";

export default function Page() {

    return(
        <div>
            <Header></Header>
            <Form></Form>
        </div>
    )
}